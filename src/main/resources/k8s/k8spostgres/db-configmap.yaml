apiVersion: v1
# Kind for kubernets ConfigMap
kind: ConfigMap
metadata:
  # Name your ConfigMap
  name: db-secret-credentials
  labels:
    app: postgresdb
data:
  # User DB
  POSTGRES_DB: pessoacrud
  # Db user
  POSTGRES_USER: lopes
  # Db password
  POSTGRES_PASSWORD: MTIzNAo=
